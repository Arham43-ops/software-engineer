'use client';
import React, { useState, useMemo } from 'react';
import { portfolioData } from '@/data/portfolio';
export default function AchievementsPage(){const [activeCategory,setActiveCategory]=useState('all');const stats=useMemo(()=>{const total=portfolioData.achievements.length;const awards=portfolioData.achievements.filter(a=>a.category==='award').length;const certifications=portfolioData.achievements.filter(a=>a.category==='certification').length+awards;const competitions=portfolioData.achievements.filter(a=>a.category==='competition').length;return{total,awards,certifications,competitions}},[]);return <main className="min-h-screen bg-background text-foreground p-8"><h1 className="text-5xl font-black mb-10">The Archive</h1><div className="flex gap-3 mb-8"><button onClick={()=>setActiveCategory('all')}>All Entries {stats.total}</button><button onClick={()=>setActiveCategory('certification')}>Certifications {stats.certifications}</button><button onClick={()=>setActiveCategory('award')}>Awards {stats.awards}</button><button onClick={()=>setActiveCategory('competition')}>Competitions {stats.competitions}</button></div><div className="grid md:grid-cols-2 xl:grid-cols-4 gap-5">{portfolioData.achievements.filter(a=>activeCategory==='all'||(activeCategory==='certification'?(a.category==='certification'||a.id==='ach-award-01'):a.category===activeCategory)).map(a=><article key={a.id} className="rounded-2xl overflow-hidden border border-border/50 bg-card"><div className="h-52 bg-muted">{a.image?.endsWith('.pdf')?<iframe src={a.image} className="w-full h-full border-0 bg-white" title={a.title}/>:a.image?<img src={a.image} alt={a.title} className="w-full h-full object-cover"/>:null}</div><div className="p-5"><div className="text-xs uppercase tracking-wider text-muted-foreground mb-2">{a.category}</div><h2 className="font-bold text-lg">{a.title}</h2><p className="text-sm text-muted-foreground mt-2">{a.issuer}</p></div></article>)}</div></main>}
